[project]
name = "oanda-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "aiofiles>=24.1.0",
    "dacite>=1.8.1",
]

[dependency-groups]
dev = [
    "openapi-python-client>=0.21.6",
    "ruff>=0.7.3",
]
